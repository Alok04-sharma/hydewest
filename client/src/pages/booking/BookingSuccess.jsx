import React from "react";
import { Link, useLocation, useParams } from "react-router-dom";
import paymentService from "../../services/payment.service";

export default function BookingSuccess(){const {bookingId}=useParams();const location=useLocation();const payment=location.state?.payment;const download=async()=>{if(!payment?._id)return;const result=await paymentService.downloadReceipt(payment._id);const url=URL.createObjectURL(result.blob);const link=document.createElement("a");link.href=url;link.download=result.fileName;link.click();URL.revokeObjectURL(url);};return <div className="grid min-h-[80vh] place-items-center bg-gradient-to-b from-emerald-50 to-white px-4"><div className="w-full max-w-xl rounded-[34px] border border-emerald-200 bg-white p-8 text-center shadow-xl"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-100 text-4xl">✓</div><h1 className="mt-5 text-3xl font-black text-slate-950">Booking confirmed!</h1><p className="mt-3 text-sm leading-6 text-slate-500">Payment successful hai. Booking confirmation, reminders aur loyalty points aapke account me update ho gaye hain.</p><div className="mt-6 flex flex-col justify-center gap-3 sm:flex-row"><Link to={`/guest/bookings/${bookingId}`} className="rounded-2xl bg-slate-950 px-5 py-3 text-sm font-black text-white">View Booking</Link>{payment?._id&&<button onClick={download} className="rounded-2xl border border-emerald-300 px-5 py-3 text-sm font-black text-emerald-700">⬇ Download Receipt</button>}</div></div></div>}